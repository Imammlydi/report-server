<%- include('../../partials/header.ejs') %>

  <!-- Page Wrapper -->
  <div id="wrapper">

    <!-- Sidebar -->
    <%- include('../../partials/sidebar.ejs') %>
    <!-- End of Sidebar -->

    <!-- Content Wrapper -->
    <div id="content-wrapper" class="d-flex flex-column">

      <!-- Main Content -->
      <div id="content">

        <!-- Topbar -->
        <%- include('../../partials/navbar.ejs') %>
        <!-- End of Topbar -->

        <!-- Begin Page Content -->
        <div class="container-fluid">
          

          <!-- Page Heading -->
          <h1 class="h3 mb-4 text-gray-800">Role Pages</h1>
          <!-- END HEADING -->
          <nav>
            <div class="nav nav-tabs" id="nav-tab" role="tablist">
              <a class="nav-item nav-link active" id="nav-home-tab" data-toggle="tab" href="#show-item" role="tab" aria-controls="nav-home" aria-selected="true">Show Item</a>
              <a class="nav-item nav-link" id="nav-profile-tab" data-toggle="tab" href="#add-item" role="tab" aria-controls="nav-profile" aria-selected="false">Add Item</a>
              <a class="nav-item nav-link" id="nav-profile-tab" data-toggle="tab" href="#add-role" role="tab" aria-controls="nav-profile" aria-selected="false">Add Role</a>
            </div>
          </nav>
          <div class="tab-content" id="nav-tabContent">

            <%- include('table_userrole.ejs') %>
            <%- include('add_modal.ejs') %>
            <%- include('add_modal_role.ejs') %>
          </div>

        </div>
        <!-- /.container-fluid -->

      </div>
      <!-- End of Main Content -->

      <!-- Footer -->
      
      <%- include('../../partials/footer.ejs') %>
      <!-- End of Footer -->

    </div>
    <!-- End of Content Wrapper -->

  </div>
  <!-- End of Page Wrapper -->

  <!-- Scroll to Top Button-->
  <a class="scroll-to-top rounded" href="#page-top">
    <i class="fas fa-angle-up"></i>
  </a>

  <!-- Logout Modal-->
  <div class="modal fade" id="logoutModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
      <div class="modal-content">
        <div class="modal-header">
          <h5 class="modal-title" id="exampleModalLabel">Ready to Leave?</h5>
          <button class="close" type="button" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">×</span>
          </button>
        </div>
        <div class="modal-body">Select "Logout" below if you are ready to end your current session.</div>
        <div class="modal-footer">
          <button class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</button>
          <a class="btn btn-primary" href="login.html">Logout</a>
        </div>
      </div>
    </div>
  </div>

  <%- include('add_modal.ejs') %>
  <%- include('add_modal_role.ejs') %>
 <%- include('edit_modal.ejs') %>
  <%- include('../../partials/js.ejs') %>


  <script>
    $('#dataTable').on('click', '.button-update', function(){
      let id = $(this).data('id');
      let name = $(this).data('name');
      let email = $(this).data('email');
      let password = $(this).data('password');
      console.log("idnyaaaa------------------",id+password)
      $('#edit-modal').modal('show');
      $('.id').val(id);
      $('.name').val(name);
      $('.email').val(email);
      $('.password').val(password);
   
    });

    $('#addModal').on('click', '.button-add', function(){
      let user = $(this).data('user');
      for(let i=0; i < user.length; i++){ 

        user[i].name
        console.log("idnyaaaa-----",user[i].name)
      }
   
      $('#add-modal').modal('show');
      $('.user').val(user);
      //$('.name').val(name);
      //$('.position').val(position);
      //$('.field').val(field);
   
    })
  </script>
  
